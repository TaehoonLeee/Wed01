<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wed01">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_home"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_home_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".memberManagement.login"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".memberManagement.find"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity
            android:name=".memberManagement.register"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity
            android:name=".MainActivityB"/>
        <activity
            android:name=".Arduino.ArduinoRegister"
            android:theme="@android:style/Theme.Dialog" />
    </application>

</manifest>
